name: NCEI Plugin Tests

on:
  pull_request:
  push:
    branches: [master]

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Setup Conda
      uses: s-weigand/setup-conda@v1
      with:
        activate-conda: false
        conda-channels: conda-forge

    - name: Python ${{ matrix.python-version }}
      shell: bash -l {0}
      # Remove the "libnetcdf<4.8.0" pin before removing the draft in the PR!
      run: |
        conda create --name TEST python=${{ matrix.python-version }} python=3 pip "libnetcdf<4.8.0" --file requirements.txt --file test_requirements.txt --strict-channel-priority
        source activate TEST
        pip install -e . --no-deps --force-reinstall
        conda info --all
        conda list

    - name: cc-plugin-glider tests
      shell: bash -l {0}
      run: |
        source activate TEST
        git clone https://github.com/ioos/cc-plugin-ncei.git
        cd cc-plugin-ncei
        conda install --file requirements.txt --file requirements-dev.txt ;
        pytest -s -rxs -v cc_plugin_ncei
